{
  "name": "webhook-server",
  "version": "1.0.0",
  "description": "Webhook server for handling alerts from AlertManager",
  "main": "webhook-server.js",
  "scripts": {
    "start": "node webhook-server.js",
    "dev": "node webhook-server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}